<ctrl:FrostyDockableWindow x:Class="DuplicationPlugin.Windows.BulkExportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ctrl="clr-namespace:Frosty.Controls;assembly=FrostyControls"
        xmlns:core="clr-namespace:Frosty.Core.Controls;assembly=FrostyCore"
        mc:Ignorable="d"
        Title="Bulk Export" Height="560" Width="520" ResizeMode="NoResize"
        FrostyLoaded="FrostyDockableWindow_FrostyLoaded" WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/FrostyControls;component/Themes/Generic.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Background="{StaticResource WindowBackground}">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="38"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Background="{StaticResource ListBackground}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <DockPanel Grid.Row="0" Margin="5" LastChildFill="True">
                <Label Content="Output Folder" FontFamily="Global User Interface" Width="90"/>
                <Button x:Name="browseButton" Content="Browse..." Width="80" DockPanel.Dock="Right"
                        Margin="4,0,0,0" Click="BrowseButton_Click"/>
                <TextBox x:Name="outputFolderTextBox" Padding="4" FontFamily="Global User Interface"
                         IsReadOnly="True" BorderThickness="1"/>
            </DockPanel>

            <DockPanel Grid.Row="1" Margin="5,0,5,5" LastChildFill="True">
                <Label Content="Skeleton" FontFamily="Global User Interface" Width="90"/>
                <Button x:Name="skeletonBrowseButton" Content="Select..." Width="80" DockPanel.Dock="Right"
                        Margin="4,0,0,0" Click="SkeletonBrowseButton_Click"/>
                <TextBox x:Name="skeletonTextBox" Padding="4" FontFamily="Global User Interface"
                         IsReadOnly="True" BorderThickness="1"/>
            </DockPanel>

            <DockPanel Grid.Row="2" Margin="5,0,5,5" LastChildFill="True">
                <Label Content="Base" FontFamily="Global User Interface" Width="90"/>
                <Label x:Name="baseHintLabel" Content="" FontFamily="Global User Interface"/>
            </DockPanel>

            <Border Grid.Row="3" Margin="5,1,5,5" BorderBrush="{StaticResource ControlBackground}" BorderThickness="0">
                <core:FrostyDataExplorer x:Name="pathSelector" ToolbarVisible="False"
                                         AssetListVisible="False" InitialHeight="0"/>
            </Border>
        </Grid>

        <Border Grid.Row="1">
            <Grid Margin="8">
                <Button x:Name="cancelButton" Content="Cancel" HorizontalAlignment="Left"
                        Width="75" Click="CancelButton_Click"/>
                <Button x:Name="exportButton" Content="Export" HorizontalAlignment="Right"
                        Width="100" Click="ExportButton_Click"/>
            </Grid>
        </Border>
    </Grid>
</ctrl:FrostyDockableWindow>
